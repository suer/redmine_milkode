<h2>Milkode</h2>

<%= form_tag :action => 'index', :project_id => @project do %>
  <%= text_field_tag :keyword, @keyword, {:autofocus=>true, :style=>'width: 300px'} %>
<% end %>
<br>
Package num: <%= @package_num %>

<% if @results %>
  keyword: <%= @keyword %><br/>
  result: <%= @results.size %><br/>
  
  <hr/>
  results:<br/>
  
  <% @results.each do |result| %>
    <%= result[:repository_identifier] %>:
    <%= link_to(File.basename(result[:path]),
      :controller => 'repositories', :action => 'entry', :id => @project, :repository_id => result[:repository_identifier], :path => result[:path]) %><br/>
  <% end %>
<% end %>
